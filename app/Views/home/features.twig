{% extends "layouts/base.twig" %}

{% block content %}
    <div class="container mx-auto px-4 py-12">
        <h1 class="text-4xl font-bold mb-4 text-center text-yellow-500">Features</h1>
        <p class="text-xl text-center text-gray-400 mb-12">Everything you need to create perfect PoE builds</p>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
            {% for feature in features %}
                <div class="bg-gray-800 p-8 rounded-lg border border-gray-700 hover:border-yellow-600 transition-all hover:transform hover:scale-105">
                    <div class="text-6xl mb-4">{{ feature.icon }}</div>
                    <h3 class="text-2xl font-bold mb-3 text-yellow-500">{{ feature.title }}</h3>
                    <p class="text-gray-300 leading-relaxed">{{ feature.description }}</p>
                </div>
            {% endfor %}
        </div>

        <div class="mt-16 text-center">
            <h2 class="text-3xl font-bold mb-6 text-yellow-500">Coming Soon</h2>
            <div class="max-w-3xl mx-auto bg-gray-800 p-8 rounded-lg">
                <ul class="space-y-3 text-left text-gray-300">
                    <li class="flex items-start">
                        <span class="text-yellow-500 mr-2">🔜</span>
                        <span>Build comparison and optimization suggestions</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-yellow-500 mr-2">🔜</span>
                        <span>Community build sharing and voting</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-yellow-500 mr-2">🔜</span>
                        <span>League starter build recommendations</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-yellow-500 mr-2">🔜</span>
                        <span>Budget optimization for builds</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-yellow-500 mr-2">🔜</span>
                        <span>Integration with trade APIs for pricing</span>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
