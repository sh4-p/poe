<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <meta name="description" content="Exile Architect - Advanced Path of Exile Build Planner with AI Integration">
    <meta name="theme-color" content="#1a1a1a">

    <title>{% block title %}Exile Architect{% endblock %} - Path of Exile Build Planner</title>

    {# Tailwind CSS CDN - Replace with compiled version in production #}
    <script src="https://cdn.tailwindcss.com"></script>

    {# Custom CSS #}
    <link rel="stylesheet" href="/assets/css/style.css">

    {# Favicon #}
    <link rel="icon" type="image/x-icon" href="/assets/images/favicon.ico">

    {# Additional head content #}
    {% block head %}{% endblock %}

    <style>
        /* Custom Tailwind configuration */
        @tailwind base;
        @tailwind components;
        @tailwind utilities;

        /* Dark theme base */
        html.dark {
            color-scheme: dark;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #0a0a0a;
            color: #e5e5e5;
        }

        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: #1a1a1a;
        }

        ::-webkit-scrollbar-thumb {
            background: #3a3a3a;
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #4a4a4a;
        }

        /* Touch-friendly minimum sizes */
        button, a, input, select {
            min-height: 44px;
        }

        /* Loading animation */
        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .animate-spin {
            animation: spin 1s linear infinite;
        }

        /* POE theme colors */
        .text-poe-unique {
            color: #af6025;
        }

        .text-poe-rare {
            color: #ffff77;
        }

        .text-poe-magic {
            color: #8888ff;
        }

        .text-poe-currency {
            color: #aa9e82;
        }

        .bg-poe-dark {
            background-color: #0a0a0a;
        }

        .bg-poe-darker {
            background-color: #050505;
        }
    </style>
</head>
<body class="bg-poe-dark min-h-screen flex flex-col">

    {# Navigation #}
    {% block navigation %}
    <nav class="bg-gray-900 border-b border-gray-800 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                {# Logo #}
                <div class="flex items-center">
                    <a href="/" class="flex items-center space-x-2">
                        <svg class="w-8 h-8 text-yellow-500" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
                        </svg>
                        <span class="text-xl font-bold text-white">Exile Architect</span>
                    </a>
                </div>

                {# Navigation Links #}
                <div class="hidden md:block">
                    <div class="flex items-center space-x-4">
                        {% if user %}
                            <a href="/dashboard" class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
                                Dashboard
                            </a>
                            <a href="/builds" class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
                                My Builds
                            </a>
                            <a href="/build/create" class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
                                Create Build
                            </a>
                            <a href="/build/import" class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
                                Import
                            </a>
                            <div class="relative group">
                                <button class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium flex items-center">
                                    {{ user.username }}
                                    <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                                    </svg>
                                </button>
                                <div class="hidden group-hover:block absolute right-0 mt-2 w-48 bg-gray-800 rounded-md shadow-lg py-1">
                                    <a href="/profile" class="block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700">Profile</a>
                                    <a href="/logout" class="block px-4 py-2 text-sm text-gray-300 hover:bg-gray-700">Logout</a>
                                </div>
                            </div>
                        {% else %}
                            <a href="/login" class="text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
                                Login
                            </a>
                            <a href="/register" class="bg-yellow-600 hover:bg-yellow-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                                Sign Up
                            </a>
                        {% endif %}
                    </div>
                </div>

                {# Mobile menu button #}
                <div class="md:hidden">
                    <button id="mobile-menu-btn" class="text-gray-300 hover:text-white p-2">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        {# Mobile menu #}
        <div id="mobile-menu" class="hidden md:hidden bg-gray-800">
            <div class="px-2 pt-2 pb-3 space-y-1">
                {% if user %}
                    <a href="/dashboard" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Dashboard
                    </a>
                    <a href="/builds" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        My Builds
                    </a>
                    <a href="/build/create" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Create Build
                    </a>
                    <a href="/build/import" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Import
                    </a>
                    <a href="/profile" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Profile
                    </a>
                    <a href="/logout" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Logout
                    </a>
                {% else %}
                    <a href="/login" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Login
                    </a>
                    <a href="/register" class="block text-gray-300 hover:text-white px-3 py-2 rounded-md text-base font-medium">
                        Sign Up
                    </a>
                {% endif %}
            </div>
        </div>
    </nav>
    {% endblock %}

    {# Flash Messages #}
    {% if flash_messages %}
    <div id="flash-messages" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        {% for type, message in flash_messages %}
        <div class="mb-4 p-4 rounded-lg {% if type == 'error' %}bg-red-900 text-red-200{% elseif type == 'success' %}bg-green-900 text-green-200{% else %}bg-blue-900 text-blue-200{% endif %}">
            <div class="flex items-center justify-between">
                <span>{{ message }}</span>
                <button onclick="this.parentElement.parentElement.remove()" class="text-white hover:text-gray-200">
                    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                    </svg>
                </button>
            </div>
        </div>
        {% endfor %}
    </div>
    {% endif %}

    {# Main Content #}
    <main class="flex-grow">
        {% block content %}{% endblock %}
    </main>

    {# Footer #}
    {% block footer %}
    <footer class="bg-gray-900 border-t border-gray-800 mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                {# About #}
                <div>
                    <h3 class="text-white font-bold mb-4">Exile Architect</h3>
                    <p class="text-gray-400 text-sm">
                        Advanced Path of Exile build planner with AI integration.
                        Create, share, and optimize your builds.
                    </p>
                </div>

                {# Quick Links #}
                <div>
                    <h3 class="text-white font-bold mb-4">Quick Links</h3>
                    <ul class="space-y-2 text-sm">
                        <li><a href="/" class="text-gray-400 hover:text-white">Home</a></li>
                        <li><a href="/builds" class="text-gray-400 hover:text-white">Browse Builds</a></li>
                        <li><a href="/build/create" class="text-gray-400 hover:text-white">Create Build</a></li>
                        <li><a href="/build/import" class="text-gray-400 hover:text-white">Import from POB</a></li>
                    </ul>
                </div>

                {# Legal #}
                <div>
                    <h3 class="text-white font-bold mb-4">Legal</h3>
                    <ul class="space-y-2 text-sm">
                        <li><a href="/privacy" class="text-gray-400 hover:text-white">Privacy Policy</a></li>
                        <li><a href="/terms" class="text-gray-400 hover:text-white">Terms of Service</a></li>
                    </ul>
                    <p class="text-gray-500 text-xs mt-4">
                        Path of Exile © Grinding Gear Games
                    </p>
                </div>
            </div>

            <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-500 text-sm">
                <p>Built with ⚔️ for the Path of Exile community</p>
                <p class="mt-2">Version 1.0.0 | Last Updated: 2025-11-20</p>
            </div>
        </div>
    </footer>
    {% endblock %}

    {# Toast Container for notifications #}
    <div id="toast-container" class="fixed top-4 right-4 z-50 space-y-2"></div>

    {# JavaScript #}
    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-btn')?.addEventListener('click', function() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });

        // Auto-dismiss flash messages
        setTimeout(function() {
            const flashMessages = document.getElementById('flash-messages');
            if (flashMessages) {
                flashMessages.style.transition = 'opacity 0.5s';
                flashMessages.style.opacity = '0';
                setTimeout(() => flashMessages.remove(), 500);
            }
        }, 5000);
    </script>

    {# Page-specific scripts #}
    {% block scripts %}{% endblock %}

</body>
</html>
